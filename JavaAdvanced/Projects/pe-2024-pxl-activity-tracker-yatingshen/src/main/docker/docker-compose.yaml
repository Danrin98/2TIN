version: "3.3"

services:
  activityTracker-db:
    image: mysql:latest
    ports:
       - "3306:3306"
    environment:
      MYSQL_DATABASE: 'activityTracker-db'
      MYSQL_USER: 'user'
      MYSQL_PASSWORD: 'password'
      MYSQL_ROOT_PASSWORD: 'admin'
